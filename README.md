# SCT_SD_3
The code defines expandLine() to stretch grid-border strings for drawing a larger Sudoku board layout.  It prepares formatted border lines (line0 to line4) using Unicode box-drawing characters.  It converts each number in the Sudoku board into display symbols from a predefined list.
